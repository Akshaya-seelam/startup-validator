{
  "name": "startup-validator",
  "version": "1.0.0",
  "description": "Startup Idea Validator Application",
  "scripts": {
    "build": "cd frontend_original && npm install && npm run build",
    "start": "cd Backend && node server.js"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "keywords": [
    "startup",
    "validator",
    "react",
    "nodejs"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
